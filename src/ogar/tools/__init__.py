"""
ogar.tools

LangGraph tools that agents can invoke.

This package will hold tool definitions (functions decorated with
@tool or wrapped in ToolSpec) that the cluster and supervisor
agents can call via LangGraph's ToolNode.

No tools implemented yet — this is a placeholder package.
Tools will be added as the agent classify node gets real logic.
"""